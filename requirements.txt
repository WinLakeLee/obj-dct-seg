# Intel RealSense SDK for Python
-- This file delegates to the core requirements. Use the optional vision extras if needed.

-r requirements-core.txt

# Optional vision extras (see requirements-vision.txt)
# To install vision extras in the same venv: python -m pip install -r requirements-vision.txt
